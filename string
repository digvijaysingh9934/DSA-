#include <iostream>
#include <string>
using namespace std;

int main() {
	string a;
    string b;
    cin>>a>>b;
    int len = a.size();
    int k = b.size();
    cout<<len<<" "<<k<<endl;
    string c=a+b;
    cout<<c<<"\n";
    
    swap(a[0],b[0]);
    cout<<a<<" "<<b;
  
    return 0;
}
